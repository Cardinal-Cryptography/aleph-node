---
name: Build and push cliain docker image

on:
  push:
    branches:
      - 'main'
      - 'release-*'
  pull_request:
    paths:
      - bin/cliain/**
  merge_group:
  workflow_dispatch:

jobs:
  main:
    name: Build and push cliain docker image
    uses: ./.github/workflows/_build-and-push-cliain.yml
