[package]
name = "primitives"
version = "0.1.0"
authors = ["Michał Seweryn <michal.seweryn@cardinals.cc>"]
edition = "2018"

[dependencies]
sp-runtime = { version = "3.0.0", default-features = false }
sp-core = { version = "3.0.0", default-features = false }

[features]
default = ["std"]
std = [
    "sp-core/std",
]

